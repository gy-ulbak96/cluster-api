apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        # - image: gcr.io/k8s-staging-cluster-api/kubeadm-bootstrap-controller:main
        - image: registry.arc.hcloud.io/bgrtest/kubeadm-bootstrap-controller-amd64:dev
          name: manager
