apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        - image: registry.arc.hcloud.io/bgrtest/k8x-staging-cluster-api/kubeadm-bootstrap-controller-amd64:dev
          name: manager
